version: 2
jobs:
  build:
    docker:
      - image: debian:stretch
    steps:
      - checkout
      - run:
          name: Test 
          command: |
            cd ..
            zip -r project.zip project
            ls 
deployment:
  staging:
    branch: master
    command: |
       ls
       cd ..
       ls
