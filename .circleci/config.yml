version: 2
jobs:
  build:
    docker:
      - image: debian:stretch
    steps:
      - checkout
      - run:
         name: Install Zip 
         command: |
            apt-get update -y 
            apt-get install -y zip 
      - run:
          name: Test 
          command: |
            cd ..
            zip -r "$CIRCLE_BUILD_NUM/project.zip"
            ls 
deployment:
  master:
    branch: master
    command: |
       ls
       cd ..
       ls
