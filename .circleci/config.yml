version: 1
jobs:
  build:
    docker:
      - image: debian:stretch
    steps:
      - checkout
      - run:
         name: Install Zip 
         command: |
            apt-get update -y 
            apt-get install -y zip 
      - run:
          name: Test 
          command: |
            cd ..
            zip -r "site-bundle-$CIRCLE_BUILD_NUM.zip" project
            ls 
deployment:
  master:
    branch: master
    command: |
       ls
       cd ..
       ls
