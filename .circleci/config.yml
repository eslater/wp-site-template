version: 2
jobs:
  build:
    docker:
      - image: debian:stretch
    steps:
      - checkout
      - run:
          name: Create Zip 
          command: 
            - ls
            - pwd
            - cd ..
            - pwd
            - touch test
            - ls 
