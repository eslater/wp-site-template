version: 2
jobs:
  build:
    docker:
      - image: debian:stretch
    steps:
      - checkout
      - run:
         name: Install Zip 
         command: |
            apt-get update -y 
            apt-get install -y zip 
      - run:
          name: Test 
          command: |
            cd ..
            zip -r project.zip project
            ls 
deployment:
  staging:
    branch: master
    command: |
       ls
       cd ..
       ls
